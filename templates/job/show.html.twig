{% extends 'baseAdmin.html.twig' %}

{% block title %}Show{% endblock %}

{% block body %}
    <h1>Show</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ job.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn" href="{{ path('job_index') }}">Retour</a>

    <a class="btn" href="{{ path('job_edit', {'id': job.id}) }}">Editer</a>

    {{ include('job/_delete_form.html.twig') }}
{% endblock %}
